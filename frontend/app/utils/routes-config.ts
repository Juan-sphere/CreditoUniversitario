/**
 * Rutas públicas exactas (no requieren autenticación)
 */
export const RUTAS_PUBLICAS = ["/", "/registro"];

/**
 * Prefijos de rutas públicas (todas las subrutas también son públicas)
 */
export const PREFIJOS_PUBLICOS = ["/analista"];
